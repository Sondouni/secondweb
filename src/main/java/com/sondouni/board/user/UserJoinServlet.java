package com.sondouni.board.user;import com.sondouni.board.model.UserVO;import javax.servlet.ServletException;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.io.IOException;@WebServlet("/user/join")public class UserJoinServlet extends HttpServlet {    @Override    protected void doGet(HttpServletRequest req, HttpServletResponse res) throws ServletException, IOException {//        req.getRequestDispatcher("/WEB-INF/View/user/join.jsp").forward(req,res);        MyUtils.disForward(req,res,"user/join");    }    @Override    protected void doPost(HttpServletRequest req, HttpServletResponse res) throws ServletException, IOException {        UserVO vo = new UserVO();        vo.setUid(req.getParameter("uid"));        vo.setUpw(req.getParameter("upw"));        vo.setNm(req.getParameter("nm"));        vo.setGender(MyUtils.getParameterInt(req,"gender"));        int result = UserDAO.join(vo);        switch (result){            case 0:                //1번째 방법//                res.sendRedirect("join");                //2번째 방법 (추천)                req.setAttribute("msg","회원가입에 실패하였습니다.");                doGet(req,res);                break;            case 1:                res.sendRedirect("/user/login");                break;        }    }}