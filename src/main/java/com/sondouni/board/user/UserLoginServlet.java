package com.sondouni.board.user;import com.sondouni.board.model.UserVO;import javax.servlet.ServletException;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import javax.servlet.http.HttpSession;import java.io.IOException;@WebServlet("/user/login")public class UserLoginServlet extends HttpServlet {    @Override // 화면보여줌    protected void doGet(HttpServletRequest req, HttpServletResponse res) throws ServletException, IOException {        MyUtils.disForward(req,res,"user/login");    }    @Override // 처리담당    protected void doPost(HttpServletRequest req, HttpServletResponse res) throws ServletException, IOException {        UserVO vo = new UserVO();        vo.setUid(req.getParameter("uid"));        vo.setUpw(req.getParameter("upw"));        int result = UserDAO.login(vo);        System.out.println(result);        switch (result){            case 0:                req.setAttribute("msg","fail to connection");                doGet(req,res);                break;            case 1:                //session로그인 처리//                vo.setUid(null);  나는 id쓸래                HttpSession session = req.getSession();                session.setAttribute("loginUser",vo);                res.sendRedirect("/board/list");                break;            case 2:                req.setAttribute("msg","check your id");                doGet(req,res);                break;            case 3:                req.setAttribute("msg","check your pw");                doGet(req,res);                break;        }    }}